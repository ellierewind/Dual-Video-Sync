The `libpgs-js-test.sup` fixture is copied from the MIT-licensed
[`Arcus92/libpgs-js`](https://github.com/Arcus92/libpgs-js) test suite.

It contains three deterministic 128x64 PGS frames used to verify real
lossless MKV extraction and libbitsub rendering without requiring a user's
media file.
