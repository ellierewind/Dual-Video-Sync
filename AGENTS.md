# Repository Instructions

- After every code change, build the Windows distribution with `npm run dist:win` from the `electron` directory.
- Do not run the Windows distribution build for plain discussion, status updates, explanations, or other requests that do not change files.
